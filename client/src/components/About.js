import React from 'react';

const Navbar = () => {
    return (
        <div>
            <h1 className="display-3">Activity Tracker</h1>
            <p className="lead">This app holds an activity log of quarantine activities done by various users. You can create a user, create an activity log and edit it at any time, and also view all logs groupped together.</p>
        </div>
    );
}

export default Navbar;